import numpy   
import time
from math import sqrt
A = int(input("Digite um numero para saber sua raiz: "))

h = sqrt(A)
print(f"A raiz de {A} é {h}")

a = numpy.zeros(10000)
print(f'conteudo {a}')

start_time = time.time()